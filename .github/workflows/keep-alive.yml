name: Keep Render Alive

on:
  schedule:
    - cron: '*/10 * * * *'   # هر ۱۰ دقیقه

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render App
        run: curl -s https://telegram-downloader1-ci81.onrender.com/ > /dev/null
